if [ -n "$BASH_VERSION" ]; then
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
fi

export VARNISHSENTRY_CONFIG=/vagrant/extras/config/varnishsentry.dev.conf

source $HOME/virtualenv/bin/activate
